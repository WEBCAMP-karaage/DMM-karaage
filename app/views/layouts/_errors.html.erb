<% if obj.errors.any? %>
  <div class="alert alert-danger" role="alert">
    <h3><%= pluralize(obj.errors.count, "件") %>  のエラーがあります。</h3>
    <ul>
      <% obj.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>