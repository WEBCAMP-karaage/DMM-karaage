<div class="container">
  <div class="row">
    <div class="col-md-2 mx-auto mt-5 ">
      <table class="table table-sm">
        <thead>
          <tr>
            <th>ジャンル検索</th>
          </tr>
        </thead>
        <tbody>
          <% @genres.each do |genre| %>
          <tr>
            <td><%= genre.name %></td>
          </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  <div class="col-md-10 mx-auto">
    <div class="mt-4 jumbotron">
      <b><p class="wellcome-message text center">
        ようこそ、ながのCAKEへ！</br>
        このサイトは、ケーキ販売のECサイトになります。</br>
        会員ではない方も商品の閲覧はできますが、</br>
        購入には会員登録が必要になります。
      </p></b>
    </div>
    <h3 class="product-headline">新着商品</h2>
    <div class="product-box  d-flex flex-wrap">
      <% @products.each do |product|%>
      <div class="flex_container">
        <table class="table table-borderless table-sm">
          <tr>
            <td><%= link_to attachment_image_tag(product, :image, :fill,200,200, fallback: "no_image.jpg", size:'200x200'), product_path(product) %></td>
          </tr>
          <tr>
            <td><%= link_to product.name, product_path(product) %></td>
          </tr>
          <tr>
            <td>¥<%= product.add_tax_price.to_s(:delimited) %></td>
          </tr>
        </table>
      </div>
      <% end %>
    </div>
    <p class="text-right"><%= link_to '全ての商品を見る>',products_path %></p>
  </div>
</div>
</div>